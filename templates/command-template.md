---
description: One-line description of what this command does
argument-hint: what input to provide (e.g., "file path", "error output", "feature description")
scope: read-only | modifies-files | creates-commits | pushes
allowed-tools: Read, Grep, Glob, Bash(specific:patterns)
outputs: what this command produces (e.g., "diagnosis report", "modified files", "PR URL")
accepts: what input formats work (e.g., "file path, error output, description")
---

# Command Name

Brief framing — 1-2 sentences explaining what Claude should accomplish.

```
$ARGUMENTS
```

## Process

1. **First step** — What to do first
2. **Second step** — What to do next
3. **Third step** — What to do last

## Output

Describe the expected result format. Be specific about structure.

## Principles

- Constraint or quality requirement
- Another constraint
- A third constraint
